#
# This file has been generated by the Ibuildings QA-Tools
#
# Any custom Travis-ci configuration be added here
# This file should be added to your project repository
#

sudo: false

language: php

matrix:
  fast_finish: true
  include:
    - php: 5.3
    - php: 5.4
    - php: 5.5
    - php: 5.6
      env:
        - EXECUTE_COVERAGE=true
    - php: 7
    - php: hhvm

cache:
  directories:
    - ~/.composer

before_script:
  - echo 'date.timezone = "Europe/Amsterdam"' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - phpenv config-add .travis.php.ini
  - composer self-update
  - composer install --prefer-source

script:
  - ant

after_script:
  - if [[ $EXECUTE_COVERAGE == 'true' ]]; then php vendor/bin/coveralls -v; fi
